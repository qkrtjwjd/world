using System.Collections.Generic;
using UnityEngine;
using UnityEngine.SceneManagement;

public class RandomEncounterSystem : MonoBehaviour
{
    // 심볼 인카운터(맵에 적이 보이는 방식)로 전환함에 따라 
    // 기존의 무작위 거리 기준 인카운터 로직을 모두 삭제했습니다.
}
