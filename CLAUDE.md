# 무채색 낙원 — 프로젝트 규약

Unity 2D · Yarn Spinner.

**이 파일에는 변하지 않는 규칙만 둔다.** 진행 상황·미해결 목록·수정 대상처럼 시간이 지나면 낡는 것은 `Assets/Docs/유니티_수동작업.md`에 있다. 설계 의도와 근거는 기획 문서(A·B·C·E)에 있고, **여기 없는 판정이 필요하면 추측하지 말고 묻는다.**

이 파일이 코드와 어긋나면 **먼저 보고한다.** 규약을 근거로 멀쩡한 코드를 고치지 않는다.

---

## 0. 절대 규칙

1. **시나리오는 프로젝트의 `D_무채색_낙원_시나리오_정본.docx`가 유일한 정본이다.** 다른 D 파일·사본·발췌본은 어떤 이유로도 기준이 되지 않는다. 이 파일과 다른 문서(F·스킬·이 파일 포함)가 D와 어긋나면 **D를 따르고, 어긋난 쪽을 고친다.**
2. **이 파일에 없는 판정이 필요하면 멈추고 묻는다.** 추론으로 보완하지 않는다.
3. **게이트 기대값을 산출 결과에 맞춰 고치지 않는다.** 기대값 변경은 D 실측을 근거로, 승인을 받아서만 한다.
4. **커맨드·SFX·BGM·감정 ID를 지어내지 않는다.** 등록된 이름만 쓴다. 이름은 §4의 출처에서 직접 조회하고, 없으면 `null` + `todo`로 남긴다.
5. **`Assets/Dialogue/*.yarn`에 자동으로 덮어쓰지 않는다.** 수동 보정 레이어가 있다. diff를 보고 사람이 결정한다.
6. **D의 본문 글자를 코드 작업 중에 수정하지 않는다.** 원고 수정은 별도 승인 작업이다.
7. **판정 실패를 조용히 버리거나 다른 형태로 출력하지 않는다.** `// UNCLASSIFIED`로 드러낸다.
8. **잘 돌아가는 건 안 건드린다.** 리팩터링은 요청받았을 때만.

---

## 1. 문서 체계와 우선순위

| 축 | 문서 | 범위 |
|---|---|---|
| A | 세계관 정본 | 태원·핵·순도·인형화 원리·폭주·결계 |
| B | 캐릭터 정본 | 인물 13인. 수치는 없음 |
| C | 시스템 정본 | 게이지·전투·저장·UI·대사 분기. **시스템 수치의 단일 출처** |
| D | 시나리오 정본 | **대사·원고의 유일한 출처** |
| E | 정본 확정 문서 | 충돌 판정. A~C와 충돌하면 E를 따른다 |
| F | 구현 기준 | 구현 형태와 에셋 계획 |

- **대사·씬·화자·게이트 수치는 D가 최상위다.** F의 검증표나 `node_map.json`이 D와 다르면 D를 실측해 그쪽을 고친다.
- 시스템 수치(경계값·구간)는 C가 출처, 구현 형태는 F가 정한다.
- C가 "상세는 F를 따른다"고 하면 F를 본다. F가 "설계 근거는 C"라고 하면 C를 본다.
- **진행 상황은 어느 정본에도 없다.** `Assets/Docs/유니티_수동작업.md`를 본다.

---

## 2. 인형화 — 마스터 게이지

**경계값 31 / 61 / 81 / 100. 판정은 '이상' 기준.**

| 수치 | 단계 (표시명) | 코드 식별자 | 비고 |
|---|---|---|---|
| 0~30 | 완전한 자율 | `Autonomy` | **30은 여기다. `>= 30`으로 쓰면 한 칸 밀린다** |
| 31~60 | 균열의 시작 | `Crack` | |
| 61~80 | 역효과 | `Backfire` | |
| 81~99 | 통제 불능 | `Loss` | 무의식 파지 구간 |
| 100 | 소멸 | `Doll` | 단계 전이가 아니라 **엔딩 트리거** |

**한글은 표시명, 코드는 영문 식별자다.** enum 멤버명을 한글로 바꾸지 않는다 — 직렬화된 인스펙터 값이 깨진다.

**단일 출처는 `CorruptionManager`.** enum과 경계 상수, 구간 판정 헬퍼를 여기 두고 모든 클래스가 참조한다.
다른 파일에 경계값을 하드코딩하지 않으며, 구간 판정은 직접 비교하지 말고 헬퍼를 호출한다.

### 데모 범위

시작 20. 종료 **하한 10 / 상한 32**. 이 범위를 벗어나는 상승원을 추가하지 않는다.
마을 발각 실패·집 90초 실패에 **인형화 페널티를 붙이지 않는다**(되감기와 이중 처벌).

---

## 3. 환상/현실 심리 게이지

0~100 슬라이더. 인형화와 **별개 변수로 분리 관리**한다.

- 대사 분기 구간: **fantasy = 30 미만 / neutral = 30~69 / real = 70 이상**
- 옷·표정 세트 전환 임계값: **70**. 0~69는 환상 세트에서 색만 빠짐, 70 이상은 현실 세트로 즉시 전환
- 균열 이벤트 발동: 게이지 **10 이하**
- 인형화 81 이상: 게이지와 무관하게 환상 강제 고정. 슬라이더 잠금/흐림 표시

강제 전환: 단검 파지 → 현실 100 / 마시멜로 섭취 → 환상 100(인형화 +2~3).

> **`>= 30` `>= 70`은 심리 게이지에서는 정상값이다.** 인형화 경계(§2)와 혼동해 고치지 않는다. 어느 게이지를 보는 코드인지 먼저 확인한다.

---

## 4. Yarn 규약

| 항목 | 규약 |
|---|---|
| `$인형화` | **읽기 전용.** 변경은 `<<add_corruption N>>` 커맨드로만. yarn 내 직접 대입 금지 |
| 노드 명명 | **기존 `House_*` 유지.** C# 인스펙터에 노드명이 직렬화되어 있다. 씬 번호 기반 재명명 금지 |
| 씬↔노드 | 1씬이 여러 노드로 쪼개진 것은 의도다. `NightSequenceManager` / `KitchenTriggerCutscene`이 사이에 연출을 끼운다. **임의로 합치지 않는다** |
| 노드 매핑 | `Scenario/node_map.json`이 정한다. 파서가 추측하지 않는다. **단 그 내용은 D 실측과 일치해야 한다** |
| 서식 태그 | **yarn 텍스트에 넣지 않는다.** `<i>` `<color=` 가 산출물에 있으면 실패 처리 |
| 분기 작업 범위 | 인형화 0~30 한 버전만 채운다. 31 이상은 노드만 만들고 비운다 |
| 파일 인코딩 | yarn 일부에 UTF-8 BOM이 있다. 도구는 `utf-8-sig`로 읽는다 |

### 이름의 출처 — 목록을 이 파일에 복사하지 않는다

이름은 코드가 바뀌면 같이 바뀐다. **작업 시점에 아래에서 직접 조회한다.**

| 대상 | 출처 |
|---|---|
| Yarn 커맨드 | `[YarnCommand]` 어트리뷰트 + `AddCommandHandler` 등록 (`Dialogue/YarnCommandBridge.cs` 등) |
| 스프라이트 감정 ID | `Assets/Date/Dialogue/CharacterSpriteData.asset`의 `emotionId` |
| 포트레이트 설정 | `Assets/Resources/PortraitConfigs/*.asset` |
| SFX | AudioManager 등록 목록 |

BGM 경로 형식만 고정이다: `Resources.Load("BGM/파일명")`.

> 목록을 문서에 박아두면 개명 때 반드시 어긋나고, 규약을 따를수록 틀리게 된다. 실제로 `open_merchant_ui`가 그렇게 남아 있었다(현재 이름 `open_sol_trade`).

---

## 5. 화자와 스타일 매핑

**변환기는 대사 본문만 출력한다. 서식은 Dialogue Runner가 화자 ID 기준으로 런타임에 붙인다.**
yarn에 직접 얹으면 다음 변환 때 사라지고, `#` 이스케이프 문제도 따라온다.

D에 등장하는 화자는 아래가 전부다. 목록에 없는 이름은 통과시키지 않는다.

| D 표기 | 화자 ID | 이름창 | 서식 | 포트레이트 |
|---|---|---|---|---|
| 세라 | `세라` | 세라 | — | 왼쪽 |
| 쿠루 | `쿠루` | 쿠루 | — | 왼쪽 |
| 루 | `루` | `{$이름}` | — | **오른쪽** |
| 솔 | `솔` | 솔 | — | 왼쪽 |
| 미루 | `미루` | 미루 | — | 왼쪽 |
| 아모 | `아모` | 아모 | — | 왼쪽 |
| 부엉이 | `부엉이` | 부엉이 | — | 왼쪽 |
| 라디오(유) | `radio_yu` | 라디오 | italic + `#d4c97a` | 없음 |
| `(루):` / `루 (속으로)` | `루독백` | **빈 문자열** | italic | 없음 |

**루만 오른쪽, 나머지 전부 왼쪽. 중앙은 사용하지 않는다.**
포트레이트 스프라이트가 아직 없는 화자도 배치는 왼쪽이다. 미제작과 미배치를 구분한다.

> `루독백`은 이름창 자체를 띄우지 않는다. 루의 이름은 `{$이름}` 변수이므로 고정 문자열이 뜨면 이름 커스터마이즈와 충돌한다.

**구 명칭 `상인`은 폐기됐다.** `솔`을 쓴다.

### 독백 표기가 D 안에서 두 가지다

| 구간 | D의 표기 |
|---|---|
| 집(D-1) | `(루): '...'` — 이탤릭 문단 |
| 마을(D-2) | `루 (속으로)  "..."` — 화자 표기 형태 |

**D가 정본이므로 원고를 고치는 것이 아니라 파서가 양쪽을 받는다.** 둘 다 `루독백`으로 매핑한다.

---

## 6. 시나리오 → 게임 텍스트 변환

상세는 스킬 `muchaesaek-scenario-to-yarn`. 요지만 둔다.

**화이트리스트 방식.** 아래 4종만 출력하고 나머지는 전부 차단한다.

| 유형 | D 표기 | 출력처 |
|---|---|---|
| 대사 | 화자 + `"..."` (§5 목록의 화자만) | 대화창 |
| 독백 | `(루): '...'` 또는 `루 (속으로)  "..."` | 대화창, 이름창 없음 |
| 읽는 물건 | 쪽지 본문 (S#04D 무표기 2줄) | `notes.json` → 전용 UI |
| 나레이션 | 예외 목록만 (아래) | 대화창 |

**차단:** 이탤릭 지문 전체 / `[FILTER]` `[BGM]` `[SFX]` `[UI]` `[CAM]` `[TRIGGER]` `[목표]` `[상호작용]` / `▶`로 시작하는 줄 / `※` / `·` 에셋 목록 / `【 】` 씬 헤더 / `S#xx` 줄 / 구간 표제 `^D-\d+\.`

**판정은 docx의 서식(볼드·이탤릭 run 속성)과 문단 텍스트로 한다.**

- 입력은 반드시 docx 직접 파싱. txt 추출본에는 서식이 없다
- **패턴에 `**` 같은 마크다운 기호를 쓰지 않는다.** docx 본문 텍스트에 그런 글자는 없다
- **`▶` 판정은 볼드 여부를 보지 않는다.** D에 볼드가 빠진 줄이 있고, 서식에 의존하면 그때 샌다

### 나레이션 예외 — 현재 원고 전체에서 1줄

- **출력:** S#04D `못 본 척해야 한다. 언제나처럼. 창밖에 던지거나 삼키거나.`
- **제외:** 같은 문단에 이어지는 `그런데 손은 주머니 속에 챙긴다.` — 손 동작 연출이다. **문장 단위로 자른다**
- **근거:** 같은 씬 ▶연출이 이 문장을 "자막이 떠 있는 동안"이라고 명시
- **반대 사례:** S#10 `목소리가 갈라진다.` — ▶연출이 "화면은 아무것도 하지 않는다"고 못박았다. 이탤릭 지문으로 차단되는 것이 정답이며, 별도 규칙을 만들지 않는다

### 게이트

아래 4가지를 전부 대조한다. 하나라도 틀리면 **exit 1**. 경고로 넘기지 않는다.

| # | 검사 | 잡는 것 |
|---|---|---|
| 1 | 씬별 합계 | 누락·중복 |
| 2 | **화자별 배분** | 합계는 맞는데 화자가 밀린 경우 |
| 3 | 노드별 카운트 | 씬 안에서 노드 간 배분이 밀린 경우 |
| 4 | 앵커 대사 | 카운트가 전부 맞는데 내용이 어긋난 경우 |

**기대값은 `node_map.json`에 두고, 그 값은 D 실측에서 나온다.** 이 파일에 사본을 두지 않는다.

화자별 배분(2)은 `by_speaker`를 실제로 읽어서 검사한다. 데이터만 있고 파서가 읽지 않는 상태를 두지 않는다. 실패 메시지는 씬 합계처럼 뭉뚱그리지 말고 화자별 차이와 방향을 찍는다.

```
FAIL S#04C  세라: 기대 11 / 산출 10  (-1)
            쿠루: 기대 13 / 산출 14  (+1)
```

배분 오류는 대개 인접 화자끼리 밀리므로 +/- 방향이 원인을 바로 가리킨다.
**루독백은 별도 화자로 계상한다.** 화자대사 카운트에 합치지 않는다.

**기대값이 0인 씬에서 텍스트가 나오면 전부 지문 유출이다.** 반대로 그런 씬은 `[UI]` 목표 갱신과 `[TRIGGER]` 알림이 유일한 전달 수단이므로, 반영 전에 배선 존재를 확인한다. 없으면 반영 보류.

### 반영 순서 — 어기면 빌드에서 내용이 사라진다

1. 쪽지 전용 UI 배선 — 없으면 쿠루의 쪽지가 게임에서 완전히 안 보인다
2. 화자 스타일 매핑을 Dialogue Runner에 적용
3. 기대값 0인 씬의 트리거 배선 확인
4. 재변환 → 게이트 통과 → yarn 반영

### 차단된 대사 처리

현행 yarn에만 있고 D에 없어서 차단된 대사는 **삭제 전에 사람에게 보고한다.**
차단은 규약이 동작한 것이지 그 줄이 불필요하다는 뜻이 아니다. 다만 **D에 넣을지 말지는 사람이 정하며, 파서가 임의로 살려두지 않는다.**

---

## 7. UI 금지 항목

- **인형화 수치를 어떤 형태로도 표시하지 않는다.** 저장 슬롯 목록에도, 단계명·색·아이콘 같은 간접 표기도 금지
- **거래창에 가격·수치·등급을 표시하지 않는다.** 잔액·예치금 개념 없음
- 집 구간 90초 제한에 **타이머 UI를 띄우지 않는다.** 화면 가장자리 어두워짐·복도 축소·저음으로만 전달
- UI는 별도 층(**Screen Space - Overlay**)에 둔다. 컬러 그레이드 효과를 타지 않게

인형화는 진실의 거울, 손가락 마디 수(두 마디=0~30 / 세 마디=31 이상), 쿠루 태도, 딱딱 소리, 그림자 토끼 색으로만 전달한다.

---

## 8. 저장 시스템

매개는 **그림자 토끼**. 루가 두 팔로 끌어안는다.

- 슬롯 **3개 고정**. 표시 항목은 위치·플레이 시간뿐
- 필터·광원 **무관**. 환상/현실에서 완전히 동일하게 보이고 주변 그림자 방향을 따르지 않는다
- 배치는 **위험 앞에만**. 데모: 집 1 / 마을 **0** / 숲 2~3
- 지나쳐도 사라지지 않는다. 위험이 해소되면 사라진다
- 인형화 0~99 저장 가능, 100은 토끼 없음(배드 엔딩이 먼저 발동하므로 실질 충돌 없음)
- 중단 저장: 메뉴 → 그만두기. **전용 슬롯 1개**, 슬롯 3개와 분리. 불러온 즉시 삭제. 전투·컷씬·대화창 중 진입 불가
- 토끼로 저장하면 중단 저장을 삭제한다 (복귀 지점 2개 방지)

**재시작 시 대사 스킵:** Yarn 노드 단위 기록, 계정 단위 누적(슬롯 간 공유), 플레이어 입력으로만 발동(자동 스킵 없음). 인형화 단계에 따라 내용이 바뀌는 노드는 **노드 키에 단계를 포함**해 새 대사로 취급한다.

---

## 9. 작업 절차

1. **D를 먼저 읽는다.** 대사 수·화자·씬 구성에 대한 판단은 전부 D 실측에서 나온다. 문서나 이 파일의 숫자를 근거로 D를 의심하지 않는다. 반대로 한다.
2. **현황은 `Assets/Docs/유니티_수동작업.md`를 본다.** 이 파일에는 진행 상황이 없다.
3. **작업 전에 대상이 이미 되어 있는지 확인한다.** 문서가 "고쳐야 한다"고 적어둔 것이 이미 고쳐져 있을 수 있다. 확인 없이 착수하지 않는다.
4. **먼저 조사한다.** 리포트·진단을 돌리고 나서 고친다. 넓게 갈아엎지 말고 targeted edit + assertion.
5. **구조 결정은 코드 작성 전에 확인받는다.** 표로 정리해 accept/reject를 묻는다.
6. **규약이 코드와 어긋나면 보고부터 한다.** 이 파일을 근거로 멀쩡한 코드를 고치지 않는다. 어느 쪽이 맞는지는 사람이 정한다.
7. **D를 편집하지 않는다.** 읽기만 한다(§0-6). 승인받아 편집하게 되면: docx는 한 문단이 여러 run으로 쪼개져 있어 문단 텍스트로 검색하면 실패하므로 run을 먼저 병합한다. 한글 파일명은 셸 인용 문제가 있으니 ASCII로 복사해 처리한다. 편집 후 손대지 않은 문단이 서식까지 그대로인지 전후 대조로 증명한다.
8. **결과 보고는 산출 값과 기대 값을 나란히 놓는다.**

---

## 10. 톤 — 코드가 아니라 게임 쪽

작업 중 대사나 연출을 만질 일이 생기면 알아둘 것.

- **설명하지 않는다.** 플레이어가 먼저 느끼고 나중에 깨닫는 구조다. 설명이 붙는 순간 장치가 죽는다
- **인형화를 대사로 드러내지 않는다.** 평소에는 수치로 조용히 쌓이고 결정적 순간에만 자각시킨다(가랑비 효과)
- 루의 톤은 감탄사 없음. 신나는 게 아니라 낯설고 이상한 것이 기본
- 쿠루는 가르쳐주지 않는다. 한 박자 늦게 경고하거나 아예 안 알려준다
- 솔은 거짓말하지 않지만 전부 말하지도 않는다. 묻지 않은 것은 알아서 말하지 않는다
- 유의 라디오는 유가 알던 범위 밖을 대답하지 못한다. 솔에게는 잡음만 흐르고 솔 본인이 반응하며, 쿠루에게는 아무 반응도 없다
- 미루는 단검에 반응하지만 아모는 같은 미소로 같은 대사를 반복한다. 반응 없음이 연출이다
