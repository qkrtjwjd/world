# 보호 구역

**여기 적힌 것은 바꾸지 않는다.** 바꿔야 할 이유가 생기면 **멈추고 사용자에게 묻는다.**

「바꾼다」는 **값 · 동작 · 배치 · 수치를 고치는 것**이다.
읽기 · 참조 · 진단 · 보고는 자유다. 보호 구역을 **근거로** 다른 곳을 고치는 것도 자유다.
**바꾸는 것만 막는다.**

---

## 1. 인트로

| 대상 |
|---|
| `Assets/Dialogue/Intro.yarn` |
| `Assets/Scenes/IntroScene.unity` |
| `Assets/Scripts/System/IntroManager.cs` |

- 위 3개의 **내용 · 연출 순서 · 타이밍 · 씬 배치**를 바꾸지 않는다.
- 인트로를 **호출하는 쪽**(타이틀에서의 진입, 씬 전환 등)은 보호 대상이 아니다.
- 인트로가 참조하는 **공용 시스템**(페이드 · BGM · 게이지)을 고칠 때, 인트로의 겉보기 동작이
  달라진다면 그것도 이 문서에 걸린다. **고치기 전에 인트로에 영향이 가는지 먼저 확인한다.**

---

## 2. 게임의 전체적인 크기 — 도트 · 해상도 규격

**`CLAUDE.md` §11 전체가 보호 대상이다.**

§11 첫 줄이 이미 못을 박아 두었다 — *"데모 종료까지 변경하지 않는 고정값이다.
값 하나를 바꾸면 나머지가 전부 따라 움직인다."* 이 문서는 그 선언에 **승인 절차**를 붙인다.

보호되는 항목:

| 묶음 | 내용 |
|---|---|
| 기준 삼각형 | 내부 해상도 · PPU · 카메라 ortho size |
| 규격 상수 | 타일 · 캐릭터 캔버스 · 캐릭터 피벗 · 아이콘 · 일러 컷 · 걷기 프레임 |
| 확대 | 정수배 제한, 레터박스 |
| 카메라 | Pixel Perfect Camera 설정, 줌 `5.625 / N` 제약, 카메라 바운드 |
| UI | UI 기준 해상도, 씬 뷰포트 `y` · `height` |

```
내부 해상도 세로 ÷ 2 ÷ PPU = 카메라 ortho size
```

셋은 한 묶음이라 **하나만 따로 고칠 수 없다.** 카메라를 만지기 전에 이 식을 먼저 본다.

- ⛔ **`1920` · `1080` 같은 옛 해상도 값을 되살리지 않는다.** 한 번 걷어낸 값이다.
- **실제 값은 §11에 있다. 이 문서에 옮겨 적지 않는다.** 두 벌이 되면 반드시 어긋난다.
  값이 필요하면 §11을 연다. 이 문서와 §11이 다르면 **§11이 맞다.**
- 보호 구역 **밖**의 코드가 위 상수를 **참조만** 하는 것은 보호 대상이 아니다.

---

## 3. 건드려야 할 때 — 절차

1. **멈춘다.** 고쳐놓고 사후 보고하지 않는다.
2. 4가지를 보고한다 — **무엇을** / **왜** / **안 고치면 무엇이 깨지는지** / **대안이 있는지**.
3. **명시적 승인을 기다린다.** 침묵 · "알아서 해" · 애매한 답은 승인이 아니다. 다시 묻는다.
4. 승인받은 **그 항목만** 고친다.
   승인은 **그 건에만** 유효하다. 다음 작업으로 이어지지 않고, 같은 파일의 다른 곳으로도 번지지 않는다.

> 판단이 서지 않으면 보호 구역으로 취급하고 묻는다. `CLAUDE.md` §0-2와 같은 원칙이다.
